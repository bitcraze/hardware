# Release jobs

name: Release

on:
  workflow_dispatch:

jobs:
  build:
    uses: bitcraze/workflows/.github/workflows/basic_release.yml@05ff329562de92396520c9d8010d6c8e7e79b122
    with:
      builder_image: 'bitcraze/web-builder'
      artifacts: 'generated/docs.zip generated/md.zip'
